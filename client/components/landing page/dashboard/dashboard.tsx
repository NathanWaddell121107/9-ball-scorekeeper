import * as React from "react";
import * as Styled from "./dashboard.styled";

const Dashboard = () => {
  return <Styled.DashboardWrapper>Dashboard</Styled.DashboardWrapper>;
};

export default Dashboard;
